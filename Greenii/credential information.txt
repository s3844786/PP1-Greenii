Replace the credentials for the Google Maps API.
Replace the relevant credentials for the default email address.
Database credential information is in springboot application.properties

The database needs to be manually inserted into the ROLE.
INSERT INTO roles(name) VALUES('ROLE_USER');
INSERT INTO roles(name) VALUES('ROLE_DRIVER');
INSERT INTO roles(name) VALUES('ROLE_ADMIN');